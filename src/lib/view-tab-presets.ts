/** Default queries for editable preset tabs (users can refine and save). */

/** Unglued / Unhinged / Unstable / Unfinity-style joke sets (extend codes as needed). */
export const FUNNY_PRESET_QUERY = "sets=UGL,UNH,UND,UNF";

/**
 * Narrow starting point for Commander Challenger–style products.
 * Uses Scryfall yearly challenger deck set codes (approximate; refine per meta).
 */
export const CHALLENGER_PRESET_QUERY = "sets=Q07,Q08,Q06,Q05,Q04,Q03,Q02,Q01";
